# Experiment Log

Read before starting new experiments.

## Current Status
- Repository initialized
- Basic structure created
- Ready for dataset download and exploration

## Next Steps
1. Set up Kaggle API token (`.kaggle/access_token`)
2. Run `make download` to fetch competition data
3. Explore dataset structure and target variable
4. Build baseline models
5. Implement feature engineering pipeline
6. Run experiments and track with MLflow
7. Generate submissions

## Questions to Answer
- What is the target variable?
- What are the key feature types (categorical, numerical, text)?
- What are the most important features?
- What baseline model to start with (logistic regression, XGBoost, LightGBM)?
- What evaluation metric to optimize (AUC, F1, etc.)?
- How to handle missing values?
- What cross-validation strategy to use?

See `AGENTS.md` for daily workflow.
